<%- include("partials/header"); -%>

<h1>Home</h1>

<p><%= startingContent %></p>

<% posts.forEach(function(post){ %>
  <h1><%= post.title %></h1> <!-- we use .title and .content to show the object's value -->
  <p><%= post.content %></p> <!-- we no longer have to say the position of the array in forEach -->
<% }) %>

<%- include("partials/footer"); -%>
